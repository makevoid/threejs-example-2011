# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'livereload' do
  watch(%r{index.html})
  watch(%r{coffee/.+\.coffee})# { |m| m[1] }
  watch(%r{vendor/.+\.js})# { |m| m[1] }
end

# guard 'coffeescript', :input => 'coffee'
